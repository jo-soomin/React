* state : props 값에 따라 내부의 구현에 필요한 데이터
- 상위 컴포넌트의 state 값을 하위 컴포넌트의 props 값으로 전달 가능
* constructor : 어떠한 컴포넌트가 실행될 때 render 함수보다 먼저 실행되면서
                      해당 컴포넌트를 초기화 시켜주고싶은 코드는 constructor 안에다가 작성해야함.
- javascript 코드로 인식 시키기 위해서는 props 값으로 중괄호 {} 사용